apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongo-pv
  labels:
   type: local
spec:
  storageClassName: csi-hostpath-snapclass
  capacity:
   storage: 5Gi
  accessModes:
   - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
   path: /data/db

# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: mongo-pv
# spec:
#   capacity:
#     storage: 5Gi
#   volumeMode: Filesystem
#   accessModes:
#     - ReadWriteOnce
#   persistentVolumeReclaimPolicy: Retain
#   storageClassName: csi-hostpath-snapclass  # Använd samma lagringsklass som snapshot-lagringsklassen
#   hostPath:
#     path: /data/db
#   csi:
#     driver: hostpath.csi.k8s.io
#     volumeHandle: /data/db